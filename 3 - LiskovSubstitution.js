'use strict';

const license = (user) => {
    this.calculateFee = (user) => {

    }
}

license.prototype.PersonalLicense = (user) => {
    this.calculateFee(user)
}

license.prototype.BusinessLicense = (user) => {
    this.calculateFee(user);
}
